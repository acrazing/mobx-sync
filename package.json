{
  "name": "mobx-sync",
  "version": "0.4.1",
  "description": "A library to persist your mobx stores.",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && tsc",
    "test": "mocha --require ts-node/register --slow 1000 ./src/**/*.spec.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/acrazing/mobx-sync.git"
  },
  "keywords": [
    "mobx",
    "persist",
    "sync",
    "localStorage"
  ],
  "author": "acrazing",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/acrazing/mobx-sync/issues"
  },
  "homepage": "https://github.com/acrazing/mobx-sync#readme",
  "dependencies": {
    "mobx": "^4.1.1",
    "monofile-utilities": "^1.6.0",
    "tslib": "^1.9.0"
  },
  "devDependencies": {
    "@types/mocha": "^5.0.0",
    "@types/node": "^7.0.60",
    "mocha": "^5.0.5",
    "ts-node": "^5.0.1",
    "typescript": "^2.8.1"
  }
}
